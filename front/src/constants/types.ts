export type ProductType = {
  name: string;
  color: string[];
  price: number;
  category: string;
  src: string;
  visited: number;
};

export type OptionType = {
  name: string;
  label: string;
  value: string;
};
